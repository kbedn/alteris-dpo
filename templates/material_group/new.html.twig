{% extends 'base.html.twig' %}

{% block title %}New MaterialGroup{% endblock %}

{% block body %}
    <h1>Create new MaterialGroup</h1>

    {{ include('material_group/_form.html.twig') }}

    <a class="btn btn-primary" href="{{ path('material_group_index') }}">back to list</a>
    <a class="btn btn-dark" href="{{ path('homepage') }}">Back to /</a>
{% endblock %}
