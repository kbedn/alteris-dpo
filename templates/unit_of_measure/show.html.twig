{% extends 'base.html.twig' %}

{% block title %}UnitOfMeasure{% endblock %}

{% block body %}
    <h1>UnitOfMeasure</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ unit_of_measure.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ unit_of_measure.name }}</td>
            </tr>
            <tr>
                <th>Shortcut</th>
                <td>{{ unit_of_measure.shortcut }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('unit_of_measure_index') }}">back to list</a>

    <a class="btn btn-primary" href="{{ path('unit_of_measure_edit', {'id': unit_of_measure.id}) }}">edit</a>

    {{ include('unit_of_measure/_delete_form.html.twig') }}
{% endblock %}
